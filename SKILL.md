---
name: attention-sovereignty-workflow
protocol: Attention Sovereignty Protocol
version: 1.0.0
language: zh-CN
---

# SKILL.md
# 注意力主权工作流

## 0. 文件职责

本文件规定**如何执行任务**：从接收请求、聚焦目标、拆分委派，到深度工作、结果验收、停止和闭环。

原则与权限见 `AGENT.md`；打断规则见 `HEARTBEAT.md`；每周审计见 `REVIEW.md`。

---

## 1. 适用场景

在以下情形启用本技能：

- 写作、研究、策划、产品开发或复杂决策；
- 用户任务很多，注意力频繁切换；
- 需要多个 AI 或工具并行处理；
- 长期项目容易被紧急事务吞噬；
- 已经搜集大量信息，却迟迟无法行动；
- AI 生成很多内容，但验收负担过高；
- 用户需要从“忙碌”转向“完成”。

简单问题可直接回答，不得强行套用完整流程。

---

## 2. 标准输入

尽量从用户请求中提取：

- 目标；
- 交付物；
- 截止时间；
- 约束条件；
- 已有材料；
- 风险等级；
- 用户必须亲自作出的决定。

信息不足但不妨碍推进时，作出可撤销假设并标注。

---

## 3. 标准输出

默认输出压缩为五部分：

1. **核心结果**：已经完成或得出的关键内容；
2. **关键依据**：足以支持结果的证据或逻辑；
3. **不确定性**：仍需核实或无法确认的部分；
4. **需要人决定**：只列真正需要用户判断的节点；
5. **下一步**：一个最小、明确、可执行动作。

---

## 4. 主工作循环

```text
CAPTURE → PRIORITIZE → SIMPLIFY → DELEGATE → FOCUS
   → SYNTHESIZE → JUDGE → EXECUTE → CLOSE → REVIEW
```

中文：

```text
捕捉 → 排序 → 删减 → 委派 → 专注
   → 汇总 → 判断 → 执行 → 闭环 → 复盘
```

---

## 5. 第一步：CAPTURE｜捕捉

将所有新任务、想法和外部请求放入统一入口，不立即展开。

记录最少字段：

```markdown
- 事项：
- 来源：
- 与当前目标的关系：直接 / 间接 / 无关
- 是否阻塞当前任务：是 / 否
- 最迟处理时间：
```

核心规则：

> **捕捉念头，不追随念头。**

---

## 6. 第二步：PRIORITIZE｜排序

对事项依次提问：

1. 它是否直接服务于当前主目标？
2. 不处理是否会阻塞主任务？
3. 是否存在真实截止时间或不可逆窗口？
4. 是否必须由用户本人处理？

默认优先级：

| 类别 | 处理方式 |
|---|---|
| 重要且紧急 | AI 快速准备决策包，用户拍板 |
| 重要不紧急 | 保护深度工作时段，持续推进 |
| 不重要但紧急 | 委派、自动化或批处理 |
| 不重要且不紧急 | 删除 |

每个工作周期只保留：

```markdown
当前战役目标：
当前唯一交付物：
本周期完成标准：
```

---

## 7. 第三步：SIMPLIFY｜删减

在开始执行前，执行“最小充分检查”。

### 7.1 删除问题

- 哪些部分根本不影响交付物？
- 哪些信息只会让成果显得更完整，却不会改变结论？
- 哪些步骤可以合并？
- 哪些 Agent、工具、审批或指标没有独立价值？
- 哪些要求属于“以后也许有用”，而不是“现在必须有用”？

### 7.2 简化输出

优先级从高到低：

```text
一句明确结论
> 一张表
> 一张流程图
> 一页决策包
> 一份长报告
```

只有任务确实需要时，才升级输出复杂度。

---

## 8. 第四步：DELEGATE｜委派

### 8.1 任务分配矩阵

| 任务特征 | 默认分配 |
|---|---|
| 重复、规则明确、可逆 | AI 自动执行 |
| 需要搜索、比较、整理 | AI 执行并压缩结果 |
| 需要多方案探索 | AI 并行，人选方向 |
| 价值、伦理、审美、战略判断 | 人主导，AI 辅助 |
| 高风险、不可逆行动 | AI 准备，人授权 |

### 8.2 创建 Agent 的门槛

每增加一个 Agent，必须回答：

```markdown
为什么需要独立 Agent：
它提供的独特价值：
它与其他 Agent 的边界：
结果如何合并：
停止条件：
```

不能回答，就不创建。

### 8.3 推荐的多 Agent 结构

```text
用户
  ↓
主控 Agent
  ├─ 研究 Agent
  ├─ 执行 Agent
  ├─ 批评 Agent
  └─ 验证 Agent
  ↓
去重 / 交叉验证 / 冲突标注 / 压缩
  ↓
单一决策包
  ↓
用户判断
```

---

## 9. 第五步：FOCUS｜罗斯福冲锋

### 9.1 启动条件

必须先明确：

- 唯一主任务；
- 唯一交付物；
- 完成标准；
- 硬停止点。

### 9.2 标准冲锋流程

#### Phase A：任务锁定

```markdown
本次只完成：
完成意味着：
本次明确不做：
```

#### Phase B：AI 外围部署

把搜索、整理、核验、反例、格式和低风险执行交给 AI。

#### Phase C：人类深度工作

用户只处理最需要连续思考的核心问题，不反复查看 AI 进度。

#### Phase D：综合验收

AI 先去重、压缩、标记冲突；用户再做选择、删除与批准。

#### Phase E：闭环

形成可发布、可测试、可交付或无需重新启动思考的下一状态。

---

## 10. 第六步：SYNTHESIZE｜汇总

AI 不得把原始输出直接倾倒给用户。

汇总必须完成：

1. 删除重复；
2. 合并共同结论；
3. 区分事实、推断和建议；
4. 标记来源冲突；
5. 标记置信度与未知项；
6. 删除不影响判断的信息；
7. 将结论压缩为一个决策界面。

---

## 11. 第七步：JUDGE｜决策包

当需要人类拍板时，固定使用：

```markdown
# Decision Required
现在只需要决定：

## Options
A. 
B. 
C. （仅在确有必要时提供）

## Key Evidence
- 

## Trade-offs
| 方案 | 收益 | 成本 | 风险 | 可逆性 |
|---|---|---|---|---|

## AI Assessment
AI 当前倾向及理由。必须明确这是建议，不是事实或授权。

## Unknowns
- 

## Default if No Decision
若用户暂不决定，采用的可逆默认动作：
```

方案通常控制在 2–4 个，不制造虚假选择。

---

## 12. 第八步：EXECUTE｜执行

执行前检查：

- 是否与主目标一致；
- 是否在权限边界内；
- 是否可逆；
- 是否已经获得必要授权；
- 是否存在更简单的实现方式；
- 是否定义了验证方法。

执行后必须留下：

- 做了什么；
- 结果在哪里；
- 如何验证；
- 是否需要回滚；
- 下一步是什么。

---

## 13. 第九步：CLOSE｜停止与闭环

满足任一条件时，应停止继续生成：

1. 已达到完成标准；
2. 新信息不会实质改变结论；
3. 继续研究的边际价值显著下降；
4. 新 Agent 只会重复已有信息；
5. 进一步优化不会明显改善交付物；
6. 当前瓶颈已经从信息不足变为需要行动。

操作性边际价值：

\[
MV=预期收益-注意力成本-执行成本-协调成本
\]

若 \(MV\le 0\)，停止、删除或简化。

结束时输出：

```markdown
本周期已完成：
尚未解决：
停止原因：
下一步最小动作：
恢复工作所需上下文：
```

---

## 14. 证据阈值

防止无限研究，为任务设置证据阈值。

### 14.1 低风险、可逆任务

达到“足以采取下一步”的证据即可行动。

### 14.2 中风险任务

需要关键事实有独立核验，主要反例被检查。

### 14.3 高风险、不可逆任务

需要更严格来源、显式不确定性、人工复核和正式授权。

> 研究强度应随决策风险提高，而不是随 AI 能力无限提高。

---

## 15. 长期项目恢复协议

重新进入长期项目时，AI 先提供：

```markdown
# Context Restore
项目目标：
当前阶段：
最近完成：
未决问题：
当前阻塞：
下一步最小动作：
本次不需要重新阅读的材料：
```

目标是消除“我得先重新想一遍”的重启成本。

---

## 16. 反模式及纠正

### 无限研究

表现：不断搜集，却不行动。

纠正：设置证据阈值和硬停止点。

### Agent 膨胀

表现：简单问题创建大量 Agent。

纠正：回到最小充分复杂度。

### 生产性拖延

表现：持续优化 Prompt、模板和系统，主交付物不动。

纠正：立刻完成一个可验收成果。

### AI 认可依赖

表现：反复要求“再来一版”“还有没有更好”。

纠正：达到完成标准后停止生成，进入发布或测试。

### 人类收件箱

表现：所有 Agent 原始结果直接推给用户。

纠正：先综合成单一决策包。

### 为自动化而自动化

仅当下式成立才值得自动化：

\[
节省的长期注意力>搭建成本+维护成本+复核成本
\]

---

## 17. 最小调用模板

用户可直接输入：

```markdown
请启用注意力主权工作流。

目标：
交付物：
截止时间：
已有材料：
限制条件：
必须由我决定的事项：

请先删减不必要工作，再委派外围任务，只在关键决策点打断我。
```
